apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: backend-servicemonitor
  namespace: default         
  labels:
    release: monitoring       
spec:
  selector:
    matchLabels:
      app: backend            
  endpoints:
    - port: http-metrics           
      path: /metrics
      interval: 15s
  namespaceSelector:
    matchNames:
      - default